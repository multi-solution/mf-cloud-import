# Python
__pycache__/
*.pyc

# OS
.DS_Store

# ── 事故防止 ──────────────────────────────────
# 台帳・事業者設定（仕訳番号・金額・ファイル名を含む。公開してはならない）
.mf-cloud/

# 認証情報（万一リポジトリ内に置かれた場合の保険。正規の置き場所は ~/.mfc/）
credentials*.json

# テスト時に誤って置いた証憑ファイルの公開を防ぐ
*.pdf
*.jpg
*.jpeg
*.png

# ★画像は一切コミットしない（例外を作らない）
# 2026-08-20、docs/images/ に許可の例外を作っていたところへ作業用スクショが混ざり、
# 41枚を公開リポジトリへ流出させた。以降、画像はリポジトリに置かない方針とする。
# ドキュメントで手順を示す場合は、文章で説明する。
docs/images/
